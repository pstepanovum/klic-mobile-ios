# Xcode / generated project
Klic.xcodeproj/
*.xcworkspace/
DerivedData*/
.build/
build/
build-ios/
*.ipa
xcuserdata/
*.xcuserstate

# SwiftPM
.swiftpm/
SPM/

# macOS
.DS_Store

# CocoaPods (workspace + Podfile.lock are committed; pods are fetched)
Pods/

# Translation API key (workspace-level; must NEVER be committed — repo is public)
.translate.env
*.env
